function SliderAutomatico() {
    /*
  const $slider = document.querySelector(".container-slider");
  const $itemSlider = document.querySelectorAll(".container-slider > li");
  console.log($slider);
  $slider.scrollLeft = $itemSlider.offsetWidth;

  */
}
export default SliderAutomatico;
